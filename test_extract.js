const fs = require('fs'); const files = fs.readdirSync('public/Image/bundle').filter(f => f.endsWith('.html')); const result = files.map(f => { const content = fs.readFileSync('public/Image/bundle/' + f, 'utf8'); const texts = [...content.matchAll(/<div[^>]*>([\s\S]*?)<\/div>/g)].map(m => m[1].trim()).filter(t => t.length > 0 && !t.includes('<')); return { file: f, texts: texts }; }); console.log(JSON.stringify(result, null, 2));
